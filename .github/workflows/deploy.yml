# This workflow is disabled – GitHub Pages deployment is handled by nextjs.yml
name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "This workflow is disabled"
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled – use nextjs.yml instead"
